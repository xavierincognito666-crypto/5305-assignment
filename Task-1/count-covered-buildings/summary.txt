You are a proficient Java programmer and familiar with various algorithms.
Your task is to implement a Java code given a natural language description illustrating an algorithm and a Java signature.

- 整体思路：通过两次遍历，统计在水平（X轴）和垂直（Y轴）方向上完全被其他建筑坐标极值包围的建筑数量。
- 数据结构与初始化：创建长度为 `n + 1` 的数组 `minX, maxX, minY, maxY` 分别记录对应坐标轴上的极值。用 `Arrays.fill` 将 `minX` 和 `minY` 初始化为 `n + 1`，`maxX` 和 `maxY` 默认初始化为 0。计数器 `ans` 初始为 0。
- 核心执行逻辑：第一次遍历 `buildings` 提取每个建筑的 `[x, y]`，不断更新同一 `y` 坐标下的最小与最大 `x` 值（`minX[y]` 和 `maxX[y]`），以及同一 `x` 坐标下的最小与最大 `y` 值（`minY[x]` 和 `maxY[x]`）。第二次重新遍历 `buildings`，检查当前坐标是否严格满足被包围条件：`minX[y] < x < maxX[y]` 且 `minY[x] < y < maxY[x]`。若满足该条件，则建筑计数 `ans` 递增 1。
- 结果返回：返回最终的包围建筑统计数量 `ans`。

Please implement the function with the following template.

```java
class Solution {
    public int countCoveredBuildings(int n, int[][] buildings) {
        
    }
}
```